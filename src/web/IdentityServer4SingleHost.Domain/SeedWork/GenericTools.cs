namespace IdentityServer4SingleHost.Domain.SeedWork
{
    public static class GenericTools
    {
        //public static string FormatTitle(string input)
        //{
        //    var formattedString = "";

        //    // Ελέγχω αν ο τίτλος του κατασκεύαστη είναι κενός
        //    if(input.Trim().Equals(""))
        //        throw new CatalogDomainException("Ο τίτλος του αντικειμένου θα πρέπει να έχει περιέχομενο.");

        //    // Βρίσκω τον αριθμο των λέξεων που περιλαμβάνονται στο string
        //    var stringArray = input.Split(" ");

        //    // Θετω το σωστό format. Θα πρέπει το πρώτο γράμμα του τιτλου να είναι κεφαλαίο.
        //    foreach (var word in stringArray)
        //    {
        //        formattedString += word.FirstOrDefault().ToString().ToUpper() + word.Substring(1).ToLower() + " ";
        //    }

        //    return formattedString.Trim();
        //}
    
    }
}
